package com.letscode.Class05;

import java.util.ArrayList;
import java.util.Scanner;

public class MaxAndMinNumbers {
  {

    /*

    ArrayList<Integer> numbers = new ArrayList<>();
    Scanner scanner = new Scanner(System.in);
    int minNumber;
    int maxNumber;
    int verifyNumber;
    int media;

    for (int j = 1; j <= 5; j++) {
      System.out.println("Digite o " + j + "° número");
      int onlyNumber =+ scanner.nextInt();

      numbers.add(onlyNumber);

    }

    for (int i = 0; i < 5; i++) {

      if (numbers.get(i) > verifyNumber) {
        maxNumber = verifyNumber;
      }
      if (numbers.get(i) < verifyNumber) {
        minNumber = verifyNumber;
      }

      verifyNumber = numbers.get(i);
      media =+ numbers.get(i);
      System.out.println(media);
    }

    System.out.println("Número máximo: "+ maxNumber);
    System.out.println("Número mínimo: "+ minNumber);
    System.out.println("Média: "+ media / numbers.size());


    */
  }
}
